# pnpm 11+: project config lives here (not package.json#pnpm).
# Root-only repo — packages field omitted; root is always included.

overrides:
  # Percy still resolves fast-xml-parser@4.x without this; audit needs >=5.7.0.
  fast-xml-parser: 5.11.1
  # snyk-nodejs-lockfile-parser (via @percy/cli) pins uuid@^8; GHSA-w5hq-g745-h8pq.
  uuid: 11.1.1

allowBuilds:
  '@parcel/watcher': true
  '@percy/core': true
  '@swc/core': true
  esbuild: true
  protobufjs: true
  sharp: true
  unrs-resolver: true
